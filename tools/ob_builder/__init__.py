"""Build-time parsers for the OB database builder (NOT used at runtime).

- xml_parser:    ST CubeProgrammer XML -> OB field layout
- svd_parser:    ST SVD -> FLASH register addresses + bits
- family_constants: RM knowledge (OPTKEY + OBL behavior) per family
"""
